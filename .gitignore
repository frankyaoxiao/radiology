# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Large data / artifacts (these live outside the repo, but guard in case)
data.zip
runs/
*.pth
*.pt
*.ckpt

# Prediction CSVs + downloaded backbone weights — large, regenerable, live outside git
submissions/
cxr_model/
*.log

# Claude Code local runtime state
.claude/

# SLURM output (but NOT the slurm/ launch scripts — those are committed)
slurm_logs/
*.out
*.err
slurm-*.out

# Local tool caches
.mypy_cache/
.pytest_cache/
.ruff_cache/

# Internal docs — never push
BRIEFING_*.md

# Data
data/
