run_name: v1_3class_multiview_omnirad_s0_crossattn
runs_root: /data/artifacts/frank/misc/runs

data_root: /data/artifacts/frank/misc
labels_csv: /data/artifacts/frank/misc/labels/train2023.csv
test_ids_csv: /data/artifacts/frank/misc/labels/test_ids.csv
val_frac: 0.1
split_seed: 42

image_size: 224
crop_scale_min: 0.9
crop_scale_max: 1.0
rotation_deg: 10.0
brightness: 0.2
contrast: 0.2

model_type: radio_dino
rad_dino_path: "hf_hub:Snarcy/OmniRad-base"
head_type: cls
dropout: 0.3

target_type: 3class
loss_fn: ce

epochs: 8
batch_size_per_gpu: 16
num_workers: 8
lr_head: 1.0e-3
lr_backbone: 1.0e-6
weight_decay: 1.0e-4
warmup_ratio: 0.05
grad_clip: 1.0

eval_every_steps: 2000
primary_metric: nmse

seed: 0
log_every_steps: 20

augmentation: trivial

mv_head_kind: cross_attn

